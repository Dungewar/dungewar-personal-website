# Visitor-facing interaction copy

Do not add labels or explanatory copy that tells visitors how an interaction works (for example, “click here,” “this stays open,” or “hover to reveal”) unless the user explicitly asks for instructions. Let the interface communicate its behavior.
