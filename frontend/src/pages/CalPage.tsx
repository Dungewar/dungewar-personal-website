import { Layout } from "../components/Layout";

export function CalPage() {
  return (
    <Layout>
      <section className="empty-state">
        <span className="eyebrow">Archived experiment</span>
        <h1>CAL Simulator</h1>
        <p>This page will eventually simulate CAL logic. For now, it is resting with dignity.</p>
      </section>
    </Layout>
  );
}
