{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p . && rsync -a --exclude='node_modules/' --exclude='*.ts' --exclude='*.tsx' ./ ./dist/"
  },
  "dependencies": {
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
