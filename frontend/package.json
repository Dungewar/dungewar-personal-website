{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ../dist/",
    "build": "npm run clean && npx tsc --project ./ && rsync -a --exclude='dist/' --exclude='node_modules/' --exclude='*.ts' --exclude='*.js' ./ ../dist/"
  },
  "dependencies": {
    "tsx": "^4.20.3"
  },
  "private": true
}
