{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p . && rsync -a --exclude='dist/' --exclude='node_modules/' --exclude='*.ts' --exclude='*.tsx' ./src/ ./dist/"
  },
  "dependencies": {
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
